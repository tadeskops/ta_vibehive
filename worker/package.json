{
  "name": "tvh-worker",
  "version": "0.1.0",
  "private": true,
  "description": "Cloudflare Worker backing ta_vibehive. GitHub is source of truth; browser is a thin client.",
  "type": "module",
  "main": "src/index.ts",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "tail": "wrangler tail",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "jose": "^5.9.0"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20250101.0",
    "typescript": "^5.6.0",
    "wrangler": "^4.86.0"
  }
}
